using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Dominio.Entidades;

namespace Dominio.IRepositorio
{
    public interface INotaUsuarioRepositorio : IRepositorioBase<NotaUsuario>
    {
        //IList<NotaUsuario> RetornaTodos(int _empId);
        //NotaUsuario RetornaPorId(int id);
        //NotaUsuario RetornaPorDescricao(int _empId, string crgDescricao);
        //NotaUsuario Salvar(NotaUsuario obj);
        //NotaUsuario Excluir(NotaUsuario obj);
    }
}
